# Leave empty to call `/api/generate` on the Vite dev origin (proxy → backend).
# Or set full API origin, e.g. http://localhost:3000
VITE_BE_URL=

# Optional: override proxy target when running `nx serve frontend` (default http://localhost:3000)
# VITE_PROXY_TARGET=http://localhost:3000
